
public class Sorts {

   //O(N^2) routines:
   /*
    * Description: Sorts the list of size elements contained in arr using the selection sort algorithm
    */
   public static <Type extends Comparable<? super Type>> void selectionSort(Type[] arr, int size) {
   }
   
   /*
    * Description: Sorts the list of size elements contained in arr using the bubble sort algorithm
    */
   public static <Type extends Comparable<? super Type>> void bubbleSort(Type[] arr, int size) {
   }
   
   /*
    * Description: Sorts the list of size elements contained in arr using the insertion sort algorithm
    */
   public static <Type extends Comparable<? super Type>> void insertionSort(Type[] arr, int size) {
   }
   
   //O(NlogN) routines:
   /*
    * Description: Sorts the list of size elements contained in arr using the merge sort algorithm 
    */
   public static <Type extends Comparable<? super Type>> void mergeSort(Type[] arr, int size) {
   }
   
   /*
    * Description: Sorts the list of size elements contained in arr using the quick sort algorithm with
    * median-of-three pivot and rearrangement of the three elements (as done in class)
    */
   public static <Type extends Comparable<? super Type>> void quickSort(Type[] arr, int size) {
      //need handout for this. Mine's missing.
   }

}
